using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Help_desk_db
{
    public class DbInitializer
    {
        public static void Initialize(ApplicationContext context)
        {

        //    if (!context.Users.Any())
        //    {
        //        var users = new User[]
        //        {
        //   // new User{Login="SomeLog", Email="SomeMail"},

        //        };
        //        foreach (User s in users)
        //        {
        //            context.Users.Add(s);
        //        }
        //        context.SaveChanges();
        //    }
        //    if (!context.Groups.Any())
        //    {
        //        var groups = new Group[]
        //     {
        //   // new Group { Name = "superadmin", Slug = "slug" },
        //     };
        //        foreach (Group s1 in groups)
        //        {
        //            context.Groups.Add(s1);
        //        }
        //        context.SaveChanges();
        //    }

        //    if (!context.Permissions.Any())
        //    {
        //        var permissions = new Permission[]
        //     {
        //  //  new Permission { Name="getsmth", Slug="slug" },
        //     };
        //        foreach (Permission c in permissions)
        //        {
        //            context.Permissions.Add(c);
        //        }
        //        context.SaveChanges();
        //    }
        }
    }
}
